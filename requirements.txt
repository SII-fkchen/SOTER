# Core dependencies for SOTER (Python 3.10+).
# Versions below match the environment the released checkpoint was trained and
# evaluated with (torch 2.4.0, transformers 5.3.0); older versions within these
# ranges are expected to work but are not actively tested.

torch>=2.4.0
transformers>=4.40.1

numpy>=1.26
scikit-learn>=1.3
safetensors>=0.4
tqdm

# Required for the terminal Neural ODE / CDE extrapolation block.
torchdiffeq>=0.2.4

# ---------------------------------------------------------------------------
# Optional dependencies
# ---------------------------------------------------------------------------
# flash-attn>=2.5    # optional; CT-RoPE falls back to eager attention anyway
